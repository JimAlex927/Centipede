# Centipede frontend

Vue 3 + Vite + TypeScript 阅读型工作台。开发时先启动后端 `:7788`，再运行：

```powershell
corepack pnpm install
corepack pnpm dev
```

生产构建和类型检查：

```powershell
corepack pnpm typecheck
corepack pnpm build
```
